# 更新记录

## 0.4.1 (10)

- Room 数据库升级到 v4，新增配音平台、音色池、小说/角色音色绑定、演绎脚本与分段状态表。
- 新增 Edge TTS、Fish Audio、OpenAI-compatible TTS 三类配音平台，并支持全局与单本小说的平台选择。
- Fish Audio 支持账号音色同步、公开音色搜索、模型选择和音色池分配。
- OpenAI-compatible TTS 支持自定义 Base URL、模型、音色 ID，以及可选 `instructions` 演绎参数。
- 旁白与角色音色可显式绑定；未绑定时按角色性别、性格标签和稳定散列自动选择。
- 配音前由 LLM 为每个阅读分段生成情绪、语气、停顿、语速和音量；格式异常时自动修复请求，仍失败则使用本地规则。
- 演绎脚本和音频增加内容哈希缓存、分段状态记录、失败重试及原子替换，重新生成失败时保留旧配音。
- 小说分析新增角色重要度与原因，详情页结合关系和剧情参与度展示主要角色。
- 设置页拆分 LLM 与配音服务状态，API Key 按平台使用 Android Keystore 加密保存。
- 新增 TTS 演绎参数单元测试与 Room v3→v4 迁移测试。

## 0.4.0 (9)

- 配音设置和角色音色管理进入内部迭代版本，为多平台配音数据结构与交互做准备。

## 0.3.1 (8)

- 应用正式更名为“章境”，同步更新桌面名称、界面品牌、封面描述、分析提示、配音测试和 Neo4j 导出标识。
- 保留原有应用包名和数据库，升级安装不会丢失小说、分析、记忆、对话和配音。

## 0.3.0 (7)

- Room 数据库升级到 v3，增加角色记忆、默认记忆、会话、会话记忆链接和本地 FTS 索引。
- v2 旧聊天自动迁移到各角色“旧对话”会话，保留小说、图谱、配音状态和聊天数据。
- 分析后的剧情与关系同步为稳定 ID 记忆，重复分析保持幂等。
- 新增多会话角色对话、角色长期默认记忆和会话临时记忆。
- 新增本地中文双字词搜索、筛选、记忆推荐和严格的防剧透边界。
- 阅读内容和聊天消息支持长按保存为原文或对话记忆。
- 统一 Material 3 设计系统、深色模式、书架搜索排序和进度展示。

## 0.2.2 (6)

- 修复小说详情“更多”按钮，加入带二次确认的删除入口。
- 删除小说时完整清理关系、剧情、聊天、章节和本地配音文件。
- 配音改用临时目录构建并在成功后替换，失败时保留旧结果。
- 恢复上次阅读进度，改进角色对话错误处理与别名匹配。
- 兼容带 Markdown 或解释文字的 LLM JSON 响应，以及不支持 JSON 模式的服务。

## 0.2.1 (5)

- Edge TTS 改为 Android WebSocket 直连，无需电脑或 Python 服务。

## 0.2.0 (4)

- 增加角色对话、章节配音、图谱可视化和 Neo4j 导出。

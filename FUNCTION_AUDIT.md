# 章境 0.4.1 (10) 功能自检

自检日期：2026-07-22

| 功能 | 状态 | 验证结果 |
| --- | --- | --- |
| TXT 导入与自动分章 | 已实现 | 单元测试覆盖 UTF-8、UTF-16、GB18030、章节标题、无标题文本和大文件输入 |
| 小说分析与增量章数 | 已实现 | OpenAI-compatible 模型检测、首次 15 章和后续自定义章数；兼容非 JSON 模式服务 |
| 角色识别与重要度 | 已实现 | 真名/别名双向归并；新增 0～1 重要度与原因，详情页结合本地关系和剧情参与度展示 |
| 聊天式阅读 | 已实现 | 对白与旁白分层显示，角色别名映射，章节切换同步阅读进度 |
| 多会话角色对话 | 已实现 | 新建、切换、自动命名、重命名、清空和删除；会话消息与临时记忆隔离 |
| 记忆库与本地检索 | 已实现 | PLOT、RELATION、EXCERPT、NOTE、CHAT；FTS4 与中文双字词检索、筛选和推荐 |
| 防剧透与请求白名单 | 已实现 | 界面锁定越界记忆，服务层再次过滤；Mock 服务验证仅发送用户明确选择的记忆 |
| 图谱与 Neo4j | 已实现 | 剧情链、角色关系、保护关系、地点迁移和 Cypher 导出 |
| Room v2→v3 | 已实现 | 设备迁移测试验证旧小说、角色、聊天与记忆结构保留 |
| Room v3→v4 | 已实现 | 设备迁移测试验证旧角色数据和 Edge 音色绑定保留，并创建三类平台、音色池和演绎脚本表 |
| Edge TTS | 已实现 | Android WebSocket 直连，支持演绎语速、音量和停顿，无需电脑服务 |
| Fish Audio | 已实现 | 模型、账号/公开音色、音色池、情绪标签、音频下载和错误分类；Mock 契约测试通过 |
| OpenAI-compatible TTS | 已实现 | 自定义服务、模型、音色、可选 instructions、音频下载与错误分类；Mock 契约测试通过 |
| 旁白/角色音色解析 | 已实现 | 支持显式绑定；未绑定时按角色类型、性别、性格标签和稳定散列选择 |
| LLM 演绎导演 | 已实现 | 分批生成情绪、语气、停顿、语速与音量；异常自动重试修复并回退本地规则 |
| 配音缓存与安全替换 | 已实现 | 内容/模型/音色/演绎参数哈希缓存；分段状态持久化；失败保留旧配音 |
| 小说删除 | 已实现 | Room 级联、显式图谱/FTS 清理和章节音频目录清理 |
| API Key 本地保护 | 已实现 | LLM 和各 TTS 平台凭据分别使用 Android Keystore AES-GCM 加密 |

## 自动化结果

- JVM 单元测试：27 项通过，0 失败，0 跳过。
- Pixel 7 / Android 17 模拟器设备测试：9 项通过，0 失败，0 跳过。
- 设备测试覆盖 Room v2→v3、v3→v4、记忆幂等与中文索引、Mock OpenAI 请求白名单、多会话隔离、记忆选择/筛选、消息长按、编辑校验和错误状态。
- `testDebugUnitTest`、`connectedDebugAndroidTest`、`assembleDebug` 均成功。
- 输出 APK 版本：`0.4.1 (10)`。

## 手工联调记录

- Edge TTS 已在 Android 模拟器中完成直连检查。
- Fish Audio 已完成云端音频生成，`dist` 中保留了三段联调 MP3。
- 公开音色的可见性不代表发布或商业使用授权，正式使用前仍需核对音色许可。

## 运行限制与后续项

- 小说分析、角色对话和 LLM 演绎需要用户配置可访问的 OpenAI-compatible API。
- Fish Audio 需要有效 API Key；Edge TTS 和其他在线语音服务要求设备可访问相应端点。
- 当前本地记忆检索不调用 embeddings API，语义检索仍可作为后续可选能力。
- 图谱拖拽/缩放/时间轴、后台断点配音、完整备份恢复和正式签名发布仍属于后续路线。
